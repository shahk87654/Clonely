# UI snapshots

Snapshots are captured from the local server at desktop (`1440x1000`) and mobile (`390x844`) viewports.

Public surfaces include the landing page, login, signup, Privacy, Terms, and Acceptable Use pages. Authenticated captures include Dashboard, Reports, Billing, and Settings using a temporary Team workspace.